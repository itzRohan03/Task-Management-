{
  "name": "task-management-app",
  "version": "1.0.0",
  "description": "A full-stack Task Management application built with Node.js backend and React frontend using MongoDB for data storage and JWT for authentication.",
  "main": "index.js",
  "scripts": {
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "dev": "npm run server && npm run client",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
